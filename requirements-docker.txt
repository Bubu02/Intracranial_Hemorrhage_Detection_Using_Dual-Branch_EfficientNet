# Optimized Grid for Cloud Hosting (CPU only)
# Core Web Framework
Flask==3.1.2
gunicorn==21.2.0
Werkzeug==3.1.3

# Image Processing
opencv-python-headless==4.10.0.84
Pillow==10.4.0

# Numerical
numpy==1.26.4

# PyTorch (CPU version will be selected by Docker layer or pip flag if needed, 
# but for standard requirements we list the packages. 
# We will use --extra-index-url in Dockerfile to get the CPU version)
torch==2.5.1
torchvision==0.20.1

# Utilities
python-dotenv==1.0.0
requests==2.32.5
